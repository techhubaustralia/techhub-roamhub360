// Test stub for Next.js's `server-only` guard (which has no Node runtime entry). Importing it in
// unit tests is a no-op; the real package only enforces "not in a client bundle" at build time.
export {};
