# Shell scripts MUST stay LF — a CRLF script breaks in the Linux container ("bad interpreter" / stray
# \r in commands). This guarantees LF regardless of a checkout machine's core.autocrlf.
*.sh text eol=lf
